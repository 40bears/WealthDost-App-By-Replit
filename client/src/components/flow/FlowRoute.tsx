import React from "react";

export function FlowRoute({ element }: { name: string; element: React.ReactNode }) {
  // This component is only used for configuration under FlowRoutes
  return <>{element}</>;
}

